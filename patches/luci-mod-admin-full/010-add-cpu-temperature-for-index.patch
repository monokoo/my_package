--- a/luasrc/view/admin_status/index.htm
+++ b/luasrc/view/admin_status/index.htm
@@ -47,12 +47,13 @@
 
 		local rv = {
 			uptime     = sysinfo.uptime or 0,
-			localtime  = os.date(),
+			localtime  = os.date("%Y-%m-%d %a %H:%M:%S"),
 			loadavg    = sysinfo.load or { 0, 0, 0 },
 			memory     = meminfo,
 			swap       = swapinfo,
 			connmax    = conn_max,
 			conncount  = conn_count,
+			temperature= luci.sys.exec("cat /sys/class/thermal/thermal_zone0/temp"),
 			leases     = stat.dhcp_leases(),
 			leases6    = stat.dhcp6_leases(),
 			wifinets   = stat.wifi_networks()
@@ -637,6 +638,9 @@
 					info.loadavg[2] / 65535.0
 				);
 
+			if (e = document.getElementById('temperature'))
+				e.innerHTML = String.format('%.2f\℃' ,info.temperature / 1000);
+
 			if (e = document.getElementById('memtotal'))
 				e.innerHTML = progressbar(
 					((info.memory.free + info.memory.buffered) / 1024) + " <%:kB%>",
@@ -687,6 +691,7 @@
 			<%=pcdata(ver.luciname)%> (<%=pcdata(ver.luciversion)%>)
 		</td></tr>
 		<tr><td width="33%"><%:Kernel Version%></td><td><%=unameinfo.release or "?"%></td></tr>
+		<tr><td width="33%"><%:CPU Temperature%></td><td id="temperature">-</td></tr>
 		<tr><td width="33%"><%:Local Time%></td><td id="localtime">-</td></tr>
 		<tr><td width="33%"><%:Uptime%></td><td id="uptime">-</td></tr>
 		<tr><td width="33%"><%:Load Average%></td><td id="loadavg">-</td></tr>
